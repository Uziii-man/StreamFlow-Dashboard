services:
  backend:
    build:
      context: ./backend
    container_name: streamflow-dashboard-backend
    # ...

  frontend:
    build:
      context: ./frontend
    container_name: streamflow-dashboard-frontend
    # ...
    